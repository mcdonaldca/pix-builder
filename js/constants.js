var INIT_WIDTH = 14;
var INIT_HEIGHT = 10;
var UNIT = 16;
var MULT = 3;

/** The various creation modes */
var Modes = {
  FLOORING: 'flooring',
  WALLS: 'walls',
}

/** The kinds of cell edges */
var Edges = {
  TOP: 'top',
  RIGHT: 'right',
  BOTTOM: 'bottom',
  LEFT: 'left',
  CENTER: 'center',
}